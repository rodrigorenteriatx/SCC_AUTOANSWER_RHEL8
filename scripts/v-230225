#!/bin/bash
#Using [[:space:]] as its more posix compatible
grep -i "^[[:space:]]*Banner[[:space:]]" /etc/ssh/sshd_config | grep -v "^#" | grep -q /etc/issue && grep -Fq "You are accessing a U.S. Government (USG) Information System" /etc/issue